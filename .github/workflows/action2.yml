name: Check SFDX Deploy without Docker
on: [workflow_dispatch]
jobs:
  call-workflow-passing-data:
    uses: MichaelPStimpson/actions/.github/workflows/deploy-sf-env.yml@main
    with:
      sandbox: false
      check-only: true
      run-scan: false
      run-tests: false
    secrets:
      CONSUMER_KEY: ${{ secrets.envPAT }}
      USERNAME: ${{ secrets.envPAT }}
      URL: ${{ secrets.envPAT }}
      JWT: ${{ secrets.envPAT }}
